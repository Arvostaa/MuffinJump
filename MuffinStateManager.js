MuffinStateManager = {};

MuffinStateManager = function (game) {
    //  var pauseGame = false;
    //  var pausePanel = new PausePanel(game);
    // game.add.existing(pausePanel);
    //game.btnPause = game.add.button(20, 20, 'btnPause', this.pauseGame, this);

};
/*
MuffinStateManager.prototype.pauseGame = function (game) {
        if (!this.pauseGame) {
            // Show panel
            this.pauseGame = true;
            this.pausePanel.show(game);
        }
        if (this.pauseGame) {
            // Leaving pause
            this.pauseGame = false;
            this.pausePanel.hide(game);

            // Anim ground
            //  this.ground.autoScroll(-100, 0);

            // play the runing animation of the hero
            // this.hero.animations.currentAnim.paused = false;

            // Activate hero gravity
            //  this.game.physics.arcade.enableBody(this.hero);
            //   this.hero.body.allowGravity = true;

            // Resume the tween
            //    this.heroTween.resume();
        }
    },

    MuffinStateManager.prototype.playGame = function (game) {
        if (this.pauseGame) {
            // Hide panel
            this.pauseGame = false;
            this.pausePanel.hide(game);
        }
    }*/